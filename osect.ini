; OpenSectional chart style configuration
; Sections are [feature_key]
; All features must be specified; missing features render as bright magenta with oversized lines.
; Properties: min_zoom, max_zoom, color, r, g, b, a, line_width, border_width, dash_length, gap_length
; "color" accepts CSS color names or hex (#RRGGBB, #RRGGBBAA)
; Defaults: line_width=1, border_width=1, dash_length=0 (solid), gap_length=0

; --- Route planner ---
;
; Configures the A* router used when a route string contains the
; `?` sigil. Preference values are PREFER, INCLUDE, AVOID, or
; REJECT (multipliers 0.8, 1.0, 1.25, 1000). The "Use airways"
; toggle in the GUI overrides the navaid and WP/RP/CN/MR fix
; subtypes to INCLUDE while it is on, and applies the airway-class
; preferences below to on-airway A* edges.

[route_plan]
max_leg_length_nm = 80

; Aerodrome subtypes (SITE_TYPE_CODE)
route_waypoint_airport       = INCLUDE
route_waypoint_balloonport   = REJECT
route_waypoint_seaplane_base = REJECT
route_waypoint_gliderport    = REJECT
route_waypoint_heliport      = REJECT
route_waypoint_ultralight    = REJECT

; Navaid subtypes (NAV_TYPE). When "Use airways" is on, all of
; these are forced to INCLUDE regardless of the configured value.
route_waypoint_vor    = INCLUDE
route_waypoint_vortac = INCLUDE
route_waypoint_vordme = INCLUDE
route_waypoint_dme    = INCLUDE
route_waypoint_ndb    = INCLUDE
route_waypoint_ndbdme = INCLUDE

; VFR fix (FIX_USE_CODE = 'VFR'). The other airway-routable fix
; subtypes (WP, RP, CN, MR) are not configurable here — they are
; REJECT when "Use airways" is off and INCLUDE when it is on.
route_waypoint_vfr = INCLUDE

; Airway classes. Applied only when "Use airways" is on.
route_airway_victor = PREFER
route_airway_jet    = REJECT
route_airway_rnav   = INCLUDE
route_airway_color  = REJECT
route_airway_other  = REJECT

; Cost factor for crossing a published gap on an airway. Applied
; only when "Use airways" is on and replaces the airway-class
; preference for that single segment. INCLUDE (default) treats
; the gap-bridge as cost-neutral relative to a generic direct
; leg. PREFER makes airway-following cost-attractive even
; through gaps — useful if you want a route that names a
; specific airway to win over an alternative that switches
; airways to avoid the gap. AVOID/REJECT push A* away from
; gap-bridging when an alternate airway routing is available.
route_airway_gap = PREFER

; --- Airports (zoom thresholds, matching associated airspace class) ---

[airport_class_b]
min_zoom = 7

[airport_class_c]
min_zoom = 8

[airport_class_d]
min_zoom = 9

[airport_class_e]
min_zoom = 10

[airport_other]
min_zoom = 11

; --- Airports (colors by type) ---

[airport_towered]
color = royalblue

[airport_untowered]
color = mediumorchid

; --- Navaids ---

[navaid_vor]
min_zoom = 9
color = limegreen

[navaid_ndb]
min_zoom = 9
color = saddlebrown

; --- Fixes (zoom thresholds, by airway membership) ---

[fix_airway]
min_zoom = 9

[fix_noairway]
min_zoom = 10

; --- Fixes (colors by type) ---

[fix_wp]
color = limegreen

[fix_rp]
color = limegreen

[fix_vfr]
color = saddlebrown

[fix_cn]
color = forestgreen

[fix_mr]
color = gray

[fix_mw]
color = gray

[fix_nrs]
color = darkslategray

; --- RCO (Remote Comm Outlets) ---

[rco]
min_zoom = 11
color = teal

; --- AWOS (Automated Weather Stations) ---

[awos]
min_zoom = 11
color = teal

; --- Obstacles ---

[obstacle_1000ft]
min_zoom = 12
color = crimson

[obstacle_200ft]
min_zoom = 13
color = orange

[obstacle_low]
min_zoom = 14
color = darkgray

; --- Airways ---
; Per-prefix colors. The altitude-band filter gates which airways actually
; render — high-altitude prefixes (J/Q/Y/H) only appear when the High band
; is enabled; low prefixes only when Low is enabled; dual-band prefixes
; (M/L/BR/AR/W/N/RTE) render in either.

; Low-altitude conventional/RNAV — dark cyan on VFR
[airway_v]
min_zoom = 9
color = darkcyan

[airway_t]
min_zoom = 9
color = darkcyan

[airway_tk]
min_zoom = 9
color = darkcyan

; High-altitude — use IFR high-chart colors (black for conventional,
; dark blue for RNAV) since they only show when the High band is on
[airway_j]
min_zoom = 9
color = black

[airway_q]
min_zoom = 9
color = darkblue

[airway_y]
min_zoom = 9
color = darkblue

[airway_h]
min_zoom = 9
color = black

; Dual-band (ICAO ATS / Bahama / Atlantic / Puerto Rico) — cyan in both
; bands per VFR convention
[airway_m]
min_zoom = 9
color = darkcyan

[airway_l]
min_zoom = 9
color = darkcyan

[airway_w]
min_zoom = 9
color = darkcyan

[airway_n]
min_zoom = 9
color = darkcyan

[airway_br]
min_zoom = 9
color = darkcyan

[airway_ar]
min_zoom = 9
color = darkcyan

[airway_rte]
min_zoom = 9
color = darkcyan

; LF/MF colored airways — magenta on VFR (low-altitude)
[airway_r]
min_zoom = 9
color = darkmagenta

[airway_a]
min_zoom = 9
color = darkmagenta

[airway_g]
min_zoom = 9
color = darkmagenta

[airway_b]
min_zoom = 9
color = darkmagenta

; --- Military training routes ---

[mtr]
min_zoom = 9
color = gray

; --- Parachute jump areas ---

[pja_area]
min_zoom = 8
color = tan
dash_length = 12
gap_length = 6

[pja_point]
min_zoom = 8
color = tan

; --- Miscellaneous activity areas ---

[maa_area]
min_zoom = 8
color = tan
dash_length = 12
gap_length = 6

[maa_point]
min_zoom = 8
color = tan

; --- ADIZ boundaries ---

[adiz]
min_zoom = 6
color = orchid
line_width = 2

; --- Class airspace ---

[airspace_b]
min_zoom = 5
color = royalblue
line_width = 2

[airspace_c]
min_zoom = 6
color = darkorchid
line_width = 2

[airspace_d]
min_zoom = 6
color = royalblue
dash_length = 12
gap_length = 6

; E2: airport surface area (dashed magenta on VFR)
[airspace_e2]
min_zoom = 9
color = darkorchid
dash_length = 12
gap_length = 6

; E3: extension to Class C surface area (dashed magenta on VFR)
[airspace_e3]
min_zoom = 9
color = darkorchid
dash_length = 12
gap_length = 6

; E4: extension to Class D/E surface area (dashed magenta on VFR)
[airspace_e4]
min_zoom = 9
color = darkorchid
dash_length = 12
gap_length = 6

; E5: upward from 700'+ AGL (magenta shading on VFR, not boundary lines)
[airspace_e5]
min_zoom = 99

; E6: en route domestic (not depicted on VFR)
[airspace_e6]
min_zoom = 99

; E7: offshore (not depicted on VFR)
[airspace_e7]
min_zoom = 99

; --- ARTCC boundaries ---

; Low altitude ARTCC boundaries (relevant for VFR)
[artcc_low]
min_zoom = 3
color = midnightblue

; High altitude ARTCC boundaries (differ slightly from low)
[artcc_high]
min_zoom = 3
color = midnightblue

; Oceanic Flight Information Region (FIR). CTA/FIR polygons are drawn
; under both this style and artcc_unlimited_cta.
[artcc_unlimited_fir]
min_zoom = 3
color = midnightblue

; Oceanic Control Area (CTA). CTA/FIR polygons are drawn under both
; this style and artcc_unlimited_fir.
[artcc_unlimited_cta]
min_zoom = 99

; Oceanic Upper Control Area (UTA).
[artcc_unlimited_uta]
min_zoom = 99

; --- Special use airspace ---

[sua_prohibited]
min_zoom = 6
color = crimson
line_width = 2

[sua_restricted]
min_zoom = 6
color = orange
line_width = 2

[sua_warning]
min_zoom = 6
color = orange
line_width = 2
dash_length = 15
gap_length = 8

[sua_alert]
min_zoom = 9
color = plum
line_width = 2

[sua_moa]
min_zoom = 7
color = plum
line_width = 2
dash_length = 15
gap_length = 8

[sua_nsa]
min_zoom = 9
color = saddlebrown
line_width = 2

; --- Runways ---

[runway]
min_zoom = 10
color = silver
line_width = 3
border_width = 0

; --- Route ---

[route]
color = magenta
line_width = 3
border_width = 1

; --- TFRs ---

[tfr]
min_zoom = 6
color = crimson
line_width = 2
dash_length = 15
gap_length = 8
